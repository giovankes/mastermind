import Peg from '../Peg';

const generatePeg = (i, pegs, idVal) => {};

const CodePegs = (colors) => {
  const pegs = [];
  let idVal;
  let pegClass;

  generatePeg(i, pegs, idVal, pegClass);
};
export default CodePegs;
